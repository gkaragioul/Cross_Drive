# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Electron builder output
release/

# Native build output
native/bin/
native/*/bin/
native/*/obj/
native-bridge/bin/
native-bridge/obj/

# Extracted third-party prerequisite payloads; ship the unmodified MSI instead.
prereqs/winfsp-extract/
prereqs/_winfsp_rel.html

# Optional shipped APFS bridge (build locally or in CI; do not commit binaries)
native-bridge-bin/apfs-fuse.exe
native-bridge-bin/APFS_FUSE_SOURCE_REV.txt

# Logs
*.log
logs/

# macOS artifacts
.DS_Store

# Windows artifacts
Thumbs.db
desktop.ini

# Environment / secrets
.env
.env.*
*.pfx
*.p12

# Signing config (never commit real certs)
signing-env.ps1

# VS / Rider
.vs/
*.user
*.suo
.idea/

# Claude workspace
.claude/

# Temp / scratch
asar-check/
asar-temp/
asar-test/
deps_fresh/
prompt-master-main/

# Embedded third-party repos (not tracked as submodules)
native-bridge/apfs-fuse/
